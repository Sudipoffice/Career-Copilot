[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @career-copilot/web build"
  publish = "apps/frontend/.next"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
